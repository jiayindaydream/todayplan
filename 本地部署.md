# 本机部署说明

> 本文档已合并升级，最新部署方式（SQLite 零依赖 + 一键启动 + 跨设备访问）请看 **[部署与跨设备使用.md](./部署与跨设备使用.md)**。

快速版：

```bash
# 唯一前提：装了 Node.js v18+
bash start-mac-linux.sh        # macOS / Linux / WSL
# 或 Windows 双击 start.bat
```

启动后浏览器开 **http://localhost:8787**，注册即用。

- 数据库已从 PostgreSQL 改为 **SQLite**（`prisma_database/app.db` 单文件），无需安装任何数据库软件
- 手机/跨设备访问：见《部署与跨设备使用.md》（同 WiFi / Tailscale / cloudflared 三档方案）
