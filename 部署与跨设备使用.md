# 部署与跨设备使用

本工具已改为 **SQLite + 零数据库依赖**，解压后双击脚本即跑。数据存在单个 `prisma_database/app.db` 文件里，备份=复制该文件。

## 一、启动（任一台机器作为「主机」）

> 主机要求：装了 Node.js v18+（https://nodejs.org）。无需安装任何数据库软件。

| 系统 | 启动方式 |
|---|---|
| macOS / Linux / WSL | 双击 `start-mac-linux.sh`（或终端 `bash start-mac-linux.sh`） |
| Windows | 双击 `start.bat` |

首次启动会自动：装依赖 → 建 `.env`（自动生成 JWT 密钥）→ 建表 → 启动。启动后浏览器开 **http://localhost:8787**，注册（手机号+密码）即用。

> 让主机随开机自启：把启动脚本挂到「Windows 任务计划程序 / macOS 登录项 / Linux systemd」。需开机后台运行时，可去掉交互改为 `node src/server.js`。

## 二、跨设备访问（三档，由易到难）

### 方案 A · 同一 WiFi（最快上手，适合在家/办公室）

1. 主机起好服务（上一步）
2. 查主机内网 IP：
   - macOS：`System Settings → 网络`，或终端 `ipconfig getifaddr en0`
   - Windows：`ipconfig`，看「IPv4 地址」（如 `192.168.1.20`）
   - Linux：`hostname -I`
3. 手机连**同一 WiFi** → 浏览器访问 `http://<主机内网IP>:8787`（如 `http://192.168.1.20:8787`）
4. 手机浏览器「添加到主屏幕」→ 像 App 一样全屏使用

> 限制：仅在同一局域网内有效，出门切 4G/5G 就访问不到。需要随时随地用，看方案 B。

### 方案 B · Tailscale（推荐 · 任意网络跨设备 · 私有不入公网）

把主机和手机都接入同一个 Tailscale 账号，双方就得到一个固定私有地址，**任何网络下**都能互访，无需端口转发、不暴露公网。

1. 注册免费账号：https://tailscale.com
2. 主机装 Tailscale 客户端并登录（macOS/Windows 有图形客户端；Linux 一行 `curl -fsSL https://tailscale.com/install | sh`）
3. 手机应用商店装 Tailscale App，登同一账号
4. 在 Tailscale 管理页给主机设个名字（如 `today-plan`），或直接用其 100.x.x.x 地址
5. 手机浏览器访问 `http://today-plan:8787`（或 `http://100.x.x.x:8787`）→ 加到主屏幕

> 这是个人跨设备的最优解：数据留在你自己机器上、不进公网、不花钱、配置一次永久可用。

### 方案 C · cloudflared 隧道（可选 · 公网 HTTPS · 暴露公网请谨慎）

需要任何网络（含外人）都能访问、且接受服务暴露在公网时使用。

1. 主机装 `cloudflared`（https://github.com/cloudflare/cloudflared/releases）
2. 临时公网地址：
   ```bash
   cloudflared tunnel --url http://localhost:8787
   ```
3. 输出形如 `https://xxx-xxx.trycloudflare.com` 的地址，手机/任何设备打开即可
4. **务必**：登录用强密码；该临时地址每次重启会变，长期用建议配 Cloudflare 命名隧道。

> ⚠️ 公网暴露后任何拿到地址的人都能尝试访问登录页，请务必用强密码，必要时再加一层反向认证。

## 三、数据备份

- 手动：`bash backup.sh` → 复制到 `backups/app-YYYYMMDD.db`（保留最近 30 份）
- 自动：把 `backup.sh` 挂到系统计划任务每日跑一次
- 迁移主机：直接把整个项目目录（含 `app.db`）拷到新机器，跑 `start-mac-linux.sh` 即恢复全部数据

## 四、可选配置

| 配置 | 在哪改 |
|---|---|
| 端口 | `.env` 的 `PORT`（默认 8787） |
| JWT 密钥 | `.env` 的 `JWT_SECRET`（改后已登录的会话需重登） |
| AI 服务商/Key/模型 | 登录后「我的 → 开发者后台」（或主界面连点左上角「今日」logo 5 次），密钥存数据库、服务端调用不暴露前端 |
| 钉钉日报发送 | `.env` 填 `DINGTALK_ACCESS_TOKEN` 和 `DINGTALK_SECRET`，服务端 HMAC 加签发送 |
